"""
Experimental / parked nodes — not wired into any workflow.

Opt-in only. Modules here are kept in tree for future integration but must
be imported explicitly (never re-exported from nodes/__init__.py) so that
unused code can't fail at graph-build time.
"""
