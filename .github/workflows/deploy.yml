name: Deploy API

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Build & Deploy API
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Build & Deploy
        run: |
          set -euo pipefail
          SHA=${{ github.sha }}
          SHORT_SHA=$(echo $SHA | cut -c1-7)
          echo "Building llmmllab-api (SHA: $SHORT_SHA)..."

          # Build and push using the Makefile deploy target
          DOCKER_TAG=$SHA make deploy
